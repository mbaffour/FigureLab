{
  "name": "figurelab-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Headless-Chromium end-to-end tests for FigureLab (dev-only; the app itself stays dependency-free).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
